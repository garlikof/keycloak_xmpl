server:
  port: 8090
spring:
  application:
    name: ${APPLICATION_NAME:spring-security-keycloak}
keycloak:
  auth-server-url: http://localhost:8080
  realm: demo
  resource: backend
  public-client: true
